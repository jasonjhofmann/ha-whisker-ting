# Pinned to the HA core version this integration is tested against (HA 2026.2.3).
pytest-homeassistant-custom-component==0.13.316
syrupy==5.0.0

# Runtime requirement from custom_components/whisker_ting/manifest.json (msgpack==1.2.1).
# HA normally installs manifest requirements itself; PHACC's test harness does not,
# so it's pinned here too -- importing custom_components.whisker_ting.api otherwise
# fails at collection time via __init__ -> coordinator -> websocket -> msgpack.
msgpack==1.2.1

# Mocks aiohttp requests at the transport layer; used to fake Cognito IdP
# responses in tests/test_auth.py without a real network call.
aioresponses==0.7.6

# Linter/formatter — PINNED so CI matches local exactly. The workflow previously
# installed an unpinned "ruff", which pulled a newer release that enabled a lint
# rule the local ruff didn't, breaking CI while local passed.
ruff==0.16.1
